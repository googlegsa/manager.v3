# Test default values in connectorInstance.xml file.  Note, 'Color' property is
# missing and should come from default settings, 'RepositoryFile' is empty and
# should be set to the empty string and override the defaults, 'Username' is set
# and should overried the default, and 'googleWorkDir' is not part of the
# defaults and has to appear here.
RepositoryFile=
Username=not_default_user
googleWorkDir=/tomcat/webapps/connector-manager/WEB-INF
